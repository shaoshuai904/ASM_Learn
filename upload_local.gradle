apply plugin: 'groovy'
apply plugin: 'maven'

//group和version在后面使用自定义插件的时候会用到
group = 'com.maple.plugin'
version = '0.0.1'

uploadArchives {
    repositories {
        mavenDeployer {
            //本地的Maven地址:当前工程下
            repository(url: uri('./../local-plugin'))
        }
    }
}